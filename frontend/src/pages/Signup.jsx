/*async function handleSignup(e) {
  e.preventDefault();

  const payload = { email, password, age: Number(age), name, avatar };

  const res = await api.post("/api/user/signup", payload);

  localStorage.setItem("token", res.data.token);
} */ 
// use something like this for signing up later in the project